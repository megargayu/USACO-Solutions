#include <bits/stdc++.h>
using namespace std;

#define MAX 1000
#define pii pair<int, int>

vector<set<pii>> closestH(MAX), closestG(MAX);
int main()
{
  ifstream fin("checklist.in");
  ofstream fout("checklist.out");

  int H, G;
  fin >> H >> G;

  

  return 0;
}
